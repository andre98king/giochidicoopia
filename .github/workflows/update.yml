name: Update Games Database

on:
  schedule:
    - cron: '0 6 * * 1'   # Ogni lunedì alle 6:00 UTC
  workflow_dispatch:        # Avvio manuale dalla pagina Actions di GitHub

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write       # Necessario per fare push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.11'

      - name: Run update script
        run: python3 auto_update.py

      - name: Build static game pages
        run: python3 build_static_pages.py

      - name: Validate catalog and generated pages
        run: python3 validate_catalog.py

      - name: Commit and push changes
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add games.js games sitemap.xml data/catalog.games.v1.json
          if git diff --staged --quiet; then
            echo "Nessuna modifica da committare."
          else
            git commit -m "🤖 Auto-update database giochi"
            git push
          fi
