# Pipeline Python — dipendenze per auto_update.py e script correlati
# Installazione: pip install -r scripts/requirements.txt

# HTTP client moderno (sync + async, meglio di requests per nuovi script)
httpx>=0.27.0

# HTTP client classico (usato in fetch_free_games.py, mantenuto per compatibilità)
requests>=2.31.0

# HTML/XML parsing (usato in fetch_free_games.py + futura integrazione in adapter)
beautifulsoup4>=4.12.0
lxml>=5.0.0           # backend veloce per BeautifulSoup

# RSS/Atom feed parsing (usato in itch_catalog_source.py)
feedparser>=6.0.0

# Traduzione automatica — Google Translate gratuito senza API key
# Usato per auto-tradurre descrizioni in italiano (GameName EN → IT)
deep-translator>=1.11.0

# Retry/backoff per gestire 429 e timeout dalle API Steam/IGDB/GOG
tenacity>=9.0.0

# Browser automation per scraping siti con rendering Vue.js/React (es. Instant Gaming)
playwright>=1.44.0

# Browser Firefox stealth con fingerprint spoofing — bypassa Cloudflare Turnstile
# Usato da fetch_affiliate_prices.py per IG + GameBillet
# Setup: pip install camoufox && python3 -m camoufox fetch
camoufox>=0.4.0
